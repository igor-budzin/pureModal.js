{
  "name": "pureModal",
  "version": "0.1.0",
  "description": "Pure JavaScript plugin for creating modal windows with support for multiple independent windows",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Igor Budzinsky",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.13.0",
    "gulp": "^3.9.1",
    "gulp-autoprefixer": "^3.1.0",
    "gulp-rename": "^1.2.2",
    "gulp-sass": "^2.3.2",
    "require-dir": "^0.3.0"
  }
}
